# Trigger-less skills — triage

> Auto-generated by `bin/skill-doctor.sh --fix`. These skills have no
> clear "Use when …" trigger in their description, so auto-invocation is
> unreliable. Each needs a one-line "Use when the user wants to …" clause
> appended to its `description:`. Not a HARD failure — they still work when
> called by name.

Count: 0 of 396 skills.

_None — every skill has a trigger._

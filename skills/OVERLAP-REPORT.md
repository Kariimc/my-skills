# Skill Overlap Report

Generated by `/skill-audit` across **107 skills**. Tracks skills whose `description`
triggers could collide, so Claude's auto-selection stays predictable. Nothing here
is auto-merged — these are recommendations.

## Structural health
- **Name/dir match:** ✅ 107/107
- **Frontmatter valid (name + description):** ✅ 107/107
- **Long descriptions trimmed:** `game-assets`, `game-marketing`, `game-environment`
  (now ~520 chars). `docx` and `xlsx` are intentionally left long — their length is
  Anthropic's tuned *negative* triggering ("do NOT trigger when…"), which prevents
  false fires and should not be cut.

## Trigger-collision clusters

| Cluster | Competing skills | Notes / who should win |
|---|---|---|
| **Skill authoring** | `skill-builder`, `skill-creator` (Anthropic), `writing-skills` (superpowers) | 3 skills for "make a skill." `skill-builder` is the primary; narrow the other two to their niches. |
| **Debugging** | `debugger`, `systematic-debugging`, `root-cause-tracing` (superpowers) | `debugger` is broadest; the superpowers two are methodologies it can invoke. |
| **Testing / TDD** | `test-engineer`, `test-driven-development`, `testing-anti-patterns`, `condition-based-waiting` (superpowers), `webapp-testing` (Anthropic) | `test-engineer` is the superset front door; the rest are narrower facets. |
| **Networking** | `network-engineer`, `network-infrastructure`, `diagnostics-expert` | `network-engineer` ≈ `network-infrastructure` (both BGP/OSPF/EVPN/SD-WAN) — strongest merge candidate. `diagnostics-expert` is distinct (packet capture / hardware). |
| **Frontend / UI build** | `web-implementation`, `ui-ux-design`, `web-artifacts-builder` (Anthropic), `frontend-design` (Anthropic), `theme-factory` (Anthropic) | Biggest new ambiguity from the import. Scope: `ui-ux-design` = design systems, `web-implementation` = building features; the Anthropic three overlap both. |
| **Code review** | `code-review`, `requesting-code-review`, `receiving-code-review` (superpowers) | Mostly complementary (do / request / receive); minor verb collision on "review." |
| **Docs writing** | `coding-notes`, `doc-coauthoring` (Anthropic), `writing-skills` (superpowers) | Overlap on "write documentation." |
| **Cannabis** | `cannabis-delivery-app`, `cannabis-delivery-compliance` | Pre-existing pair: architecture vs full implementation. |
| **Game art** | `game-art`, `game-assets`, `game-environment`, `animation-particle-design` | Pre-existing cluster; roles are distinct enough but triggers brush. |
| **Marketing** | `digital-marketing`, `game-marketing` | `game-marketing` is the specialization; should win game queries. |

**Intentionally coherent (not a problem):** the superpowers "thinking" family —
`brainstorming`, `writing-plans`, `executing-plans`, `when-stuck`,
`inversion-exercise`, `collision-zone-thinking`, `scale-game`,
`meta-pattern-recognition`, `simplification-cascades`,
`preserving-productive-tensions`. A process toolkit; leave as-is.

## Ranked cleanup opportunities
1. **Skill authoring** — 3 skills for one job; consolidate or narrow.
2. **Networking** — `network-engineer` ≈ `network-infrastructure`; merge into one.
3. **Frontend/UI** — narrow the imported Anthropic UI skills so the originals win.
4. **Testing** — make `test-engineer` the front door; reword the 4 facets.
5. **Debugging** — keep `debugger` primary; frame the superpowers two as techniques.
6. **Docs writing** — disambiguate `coding-notes` vs `doc-coauthoring`.

> Recommended strategy: *narrow the imported skills' triggers* so your originals win
> ambiguous matches, rather than deleting anything. Run `/skill-audit` after changes.
